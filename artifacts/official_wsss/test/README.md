# Locked test output

Status: not evaluated.

This directory remains a fail-closed placeholder until the source, checkpoint,
split manifest, image size, and validation-selected threshold are committed in
`configs/official_wsss_frozen_test.json`. The test partition may then be
evaluated once on Kaggle, with no tuning or threshold sweep.
