# Frozen configurations

`official_wsss_frozen_test.json` is generated only after the official WSSS
source and validation evidence are committed. Schema v4 binds source-file
hashes, split manifest, WSSS checkpoint, image size 448, and validation-selected
threshold 0.85 while remaining portable between the local repository and
Kaggle.
